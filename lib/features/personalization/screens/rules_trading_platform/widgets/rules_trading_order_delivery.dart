import 'package:flutter/material.dart';
import '../../../../../common/widgets/texts/rich_helper_text.dart';
import '../../../../../generated/l10n/l10n.dart';

class TRulesTradingOrderDelivery extends StatelessWidget {
  const TRulesTradingOrderDelivery({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8.0),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          buildRichTextSimple(context, '6.1.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.2.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.3.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.4.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.4.1.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.4.2.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.4.3.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.5.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.5.1.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.5.2.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.5.3.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.5.4.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.5.5.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.6.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.6.1.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.6.2.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.6.3.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.6.4.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.7.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.8.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.9.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.9.1.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.9.2.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.9.3.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.10.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.10.1.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.10.2.', S.of(context).returnGoodsCarriedAccordance),
          const SizedBox(height: 8.0),
          buildRichTextSimple(context, '6.11.', S.of(context).returnGoodsCarriedAccordance),
        ],
      ),
    );
  }
}
