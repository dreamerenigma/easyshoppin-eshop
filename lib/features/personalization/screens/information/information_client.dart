import 'package:flutter/material.dart';

class InformationClientScreen extends StatelessWidget {
  const InformationClientScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
