import 'package:flutter/material.dart';
import '../../../../common/widgets/appbar/appbar.dart';

class FindSimilarProductsScreen extends StatelessWidget {
  const FindSimilarProductsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: TAppBar(
        titleSpacing: 0,
        showBackArrow: true,
        title: Row(

        ),
      ),
    );
  }
}
